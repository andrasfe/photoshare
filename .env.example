# PhotoShare Configuration
# Copy this file to .env and edit with your values
# Used by both server and client

# Shared secret - must match between server and client (required)
# Generate with: openssl rand -hex 32
PHOTOSHARE_SECRET=your-shared-secret-here

# Server URL - for client to connect to (required for client)
PHOTOSHARE_SERVER_URL=http://server-ip:8080

# Download directory (optional, default: ./downloads)
PHOTOSHARE_DOWNLOAD_DIR=./downloads

# Poll interval in hours (optional, default: 1)
PHOTOSHARE_POLL_INTERVAL=1

# Request timeout in seconds (optional, default: 300)
PHOTOSHARE_TIMEOUT=300
